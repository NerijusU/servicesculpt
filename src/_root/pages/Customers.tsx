import React from 'react';

const Customers = () => {
  // TODO: is authenticated === true ? <Customers /> : <Home />
  // customers belongs to users customers ? <Customers /> : <Home />
  return <div className="common-container">Customers</div>;
};

export default Customers;
